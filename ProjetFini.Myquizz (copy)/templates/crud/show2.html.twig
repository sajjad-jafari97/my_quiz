{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('app_c_r_u_d_index') }}">back to list</a>

    <a href="{{ path('app_c_r_u_d_edit', {'id': user.id}) }}">edit</a>
{% endblock %}
